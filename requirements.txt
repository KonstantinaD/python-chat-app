# Web UI Framework (like building a Blazor/Razor frontend, but in Python)
gradio==5.9.1

# Hugging Face Transformers (the AI models library)
transformers==4.47.1

# PyTorch - the deep learning backend (transformers needs this)
torch==2.5.1

# Accelerate - optimizes model loading on your hardware
accelerate==1.2.1
